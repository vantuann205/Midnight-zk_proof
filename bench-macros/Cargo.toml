[package]
name = "bench_macros"
version = "0.1.0"
edition = "2024"
license-file.workspace = true

[dependencies]
quote      = "1.0"
syn        = { version = "2.0", features = ["full"] }

[lib]
proc-macro = true